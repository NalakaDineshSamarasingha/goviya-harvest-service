spring:
  application:
    name: GOVIYA-HARVEST-SERVICE
  boot:
    admin:
      client:
        url: http://localhost:8080
        enabled: true
        auto-registration: true
  profiles:
    active: local

server:
  port: 8081
  servlet:
    context-path: /api/goviya-harvest-core

swagger:
  server-url: http://localhost:8081

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    doc-expansion: none
